{
    "name": "ICube-project",
    "version": "1.0.0",
    "scripts": {
        "build": "webpack --config webpack.config.js",  // Script pour compiler
        "test": "jest"  // Script pour lancer des tests
        },
    "dependencies": {
        "three": "^0.150.0"  // La version de Three.js
        },
    "devDependencies": {
        "webpack": "^5.0.0",
        "jest": "^27.0.0"
        }
}